#include "AudioParam.h"

// template<typename type> type& Value<type>::operator=(const type& v) {
// 	set(v);
// 	return *v;
// }

/*

template<typename type> type& Value<type>::operator+(const type& v) const {
	return *_v + v;
}

template<typename type> void Value<type>::operator+=(const type& v) {
	_updated = true;
	*_v += v;
}

template<typename type> type& Value<type>::operator-(const type& v) const {
	return *_v - v;
}

template<typename type> void Value<type>::operator-=(const type& v) {
	_updated = true;
	*_v -= v;
}

template<typename type> type& Value<type>::operator*(const type& v) const {
	return *_v * v;
}

template<typename type> void Value<type>::operator*=(const type& v) {
	_updated = true;
	*_v *= v;
}

template<typename type>	bool Value<type>::operator==(const type& v) const {
	return *_v == v;	
}

template<typename type>	bool Value<type>::operator!=(const type& v) const {
	return *_v != v;		
}

*/	


